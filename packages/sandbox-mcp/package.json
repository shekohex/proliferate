{
	"name": "proliferate-sandbox-mcp",
	"version": "0.1.19",
	"description": "MCP server for managing services in Proliferate cloud sandboxes",
	"type": "module",
	"bin": {
		"sandbox-mcp": "./dist/bundle.cjs",
		"proliferate": "./dist/proliferate.cjs"
	},
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "tsc",
		"bundle": "rm -f dist/bundle.cjs dist/proliferate.cjs && esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/bundle.cjs --banner:js='#!/usr/bin/env node\nprocess.env.NODE_ENV=process.env.NODE_ENV||\"production\";' --external:pino-pretty --external:node-pty --external:ws && esbuild src/cli/entrypoint.ts --bundle --platform=node --target=node20 --outfile=dist/proliferate.cjs --banner:js='#!/usr/bin/env node' && chmod +x dist/bundle.cjs dist/proliferate.cjs",
		"test": "vitest run",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"express": "^4.18.2",
		"node-pty": "^1.1.0",
		"ws": "^8.16.0"
	},
	"devDependencies": {
		"@proliferate/logger": "workspace:*",
		"@types/express": "^4.17.21",
		"@types/node": "^20.11.0",
		"@types/ws": "^8.5.10",
		"esbuild": "^0.27.3",
		"typescript": "^5.3.3",
		"vitest": "^4.0.18"
	},
	"files": ["dist"],
	"publishConfig": {
		"access": "public"
	}
}
