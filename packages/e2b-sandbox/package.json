{
	"name": "@proliferate/e2b-sandbox",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"prebuild:template": "pnpm --filter @proliferate/sandbox-daemon bundle && test -f ../sandbox-daemon/dist/daemon.cjs && cp ../sandbox-daemon/dist/daemon.cjs ./sandbox-daemon.cjs || (echo 'ERROR: sandbox-daemon bundle not found' && exit 1)",
		"build:template": "npx tsx build.ts"
	},
	"dependencies": {
		"@proliferate/environment": "workspace:*",
		"dotenv": "^16.3.1",
		"e2b": "^2.10.2"
	},
	"devDependencies": {
		"tsx": "^4.7.0",
		"typescript": "^5.3.3"
	}
}
